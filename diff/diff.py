import re


class Project:

    def __init__(self, pathname):
        if (pathname):
            this.pathname = pathname
            this.filename = pathname.split('/')[-1]
        else 
            raise()

    
    def diff(self, )